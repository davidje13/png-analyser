import { registerChunk } from '../../registry.mjs';

/**
 * @typedef {import('../../registry.mjs').State & {
 * }} skRfState
 * @typedef {import('../../registry.mjs').Chunk & {
 * }} skRfChunk
 */

registerChunk('skRf', { }, (/** @type {skRfChunk} */ chunk, /** @type {skRfState} */ state, warnings) => {
  // TODO
});
